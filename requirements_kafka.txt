# Kafka dependencies
kafka-python==2.0.2

# Google Gemini API
google-generativeai==0.3.2

# Environment variables
python-dotenv==1.0.0

# Additional dependencies for summary system
requests==2.31.0

# Zulip client (already in main requirements, but including for completeness)
zulip==0.9.1
